# TITULUS — state

## Proyecto
- TITULUS (titulus, -i: inscripción, rótulo, título). Generador de títulos de crédito para vídeo. Nombre elegido 2026-09-13; hasta entonces "Credit Titles Studio" / "credits app".
- Origen: app generada por z.ai, descargada como `.tar`. El `.tar` original y el backup del git de z.ai están en `.archive/zai/` (ignorado).
- Repo publico `memoriainfinita/titulus`. App desplegada en GitHub Pages: https://memoriainfinita.github.io/titulus/ (workflow `.github/workflows/pages.yml`, se despliega en cada push a `main`).

## Stack
- Next.js 16.2.9 (Turbopack), React 19, TypeScript, Tailwind 4, shadcn/ui, Zustand, html-to-image + dos codificadores de vídeo: Mediabunny (WebCodecs) y FFmpeg-wasm. Estado en cliente (Zustand + localStorage); sin backend ni base de datos.
- Gestor: pnpm 11.1.1 (Node v24.9.0). Diseñado originalmente para Bun (no instalado en esta máquina).

## Arranque
- `pnpm install` (requiere builds aprobados, ver Patterns) → `pnpm dev`.
- Dev server: http://localhost:3000.

## Arquitectura
- Layout de 3 paneles (`src/app/page.tsx`): `CreditEditor` (izq), `CreditPreview` (centro), `ConfigPanel` (der).
- Lógica de créditos en `src/lib/credit/` y `src/components/credit/`. Modos: `scroll` y `appearing`.
- Estado real en cliente con Zustand (persistido en localStorage).
- Exportación de vídeo: `useVideoExport.ts` elige motor según `ExportOptions.engine`; el diálogo lo muestra como selector "Motor".
  - Rápido (`webcodecsExport.ts`): por frame `toCanvas` → canvas intermedio de dimensiones pares → Mediabunny `CanvasSource` (H.264 `avc`, `prefer-hardware`) → MP4 en `BufferTarget`. Solo MP4. Soporte comprobado al abrir el diálogo (`canEncodeVideo`).
  - Compatible (FFmpeg-wasm, en `useVideoExport.ts`): por frame `toPng` → MEMFS → libx264/VP9. MP4 o WebM. Carga el core desde unpkg.
  - Compartido: `fontEmbed.ts` (`createFrameFontEmbedder`: CSS de Google Fonts con data URLs + `@font-face` de fuentes subidas; por frame incrusta solo las caras que usa, vía `fontSubset.ts`), `exportSettings.ts` (dimensiones pares, calidad → nivel Mediabunny, timing por frame).

## Patterns
- [pnpm] Builds nativos (sharp, swc, parcel/watcher, etc.) se aprueban en `pnpm-workspace.yaml` con `allowBuilds: <pkg>: true`. El bloque `"pnpm"` de `package.json` no lo lee pnpm 11. Confirmed 2026-06.
- [windows] Scripts npm con `| tee` fallan al lanzarse con pnpm en Windows (cmd.exe no tiene `tee`). Confirmed 2026-06.
- [export] El coste por frame lo domina la captura DOM, no el codificador: html-to-image mete `fontEmbedCSS` en el SVG de CADA frame y el navegador lo re-decodifica al cargarlo como imagen. Con el CSS completo de Google Fonts (10 familias, ~9 MB en data URLs) la carga del SVG costaba ~670 ms/frame. Incrustar solo las caras usadas lo lleva a ~100 ms. Medir con instrumentación de `HTMLImageElement.src`/`load` antes de culpar al encoder. Confirmed 2026-09.
- [export] Verificación sin la extensión de Chrome: `playwright-core` en el scratchpad con `channel: "chrome"` (Chrome instalado), `showSaveFilePicker = undefined` por `addInitScript` para forzar la descarga, y `ffprobe`/`ffmpeg -lavfi ssim` sobre el MP4. Para cargar un proyecto de prueba, sembrar `localStorage["titulus-store"]` con `addInitScript` en una página nueva: escribirlo y recargar no sirve, la app lo sobrescribe. Confirmed 2026-09.

## History
- 2026-06-22: extraído tar de z.ai. `.env` corregido (`DATABASE_URL` a `file:./db/custom.db`). Migrado de npm a pnpm. Script `dev` arreglado (quitado `| tee dev.log`). Builds aprobados vía `pnpm-workspace.yaml`. App arrancada OK (HTTP 200). Git iniciado limpio en `app/` (historial z.ai descartado por decisión del usuario). Commit inicial `7e0db66`.
- 2026-06-22: arreglado scroll de paneles izq/der con `min-h-0` en `ScrollArea` (`ConfigPanel.tsx`, `CreditEditor.tsx`). Commit `eb2f4cf`. Añadidos al TODO los features/bugs pedidos por el usuario (timing por item, presets propios, modo oscuro, timeline, control de fade, retardo inicial de scroll, lentitud de exportación, botones de navegación visibles en el vídeo, auditoría de privacidad). Log de sesión en `state.md` commit `2e6473f`.
- 2026-06-22: eliminada dependencia muerta `z-ai-web-dev-sdk` (no se importaba); lockfile actualizado. Commit `195cf4b`. Creado `README.md` del proyecto. Commit `1a6abee`.
- 2026-06-23: arreglado bug del primer texto en scroll (eliminado padding superior duplicado en `ScrollCredits.tsx`). Implementado modo oscuro de la UI con `next-themes` (`theme-provider.tsx`, `theme-toggle.tsx`, wrap en `layout.tsx`, toggle en `page.tsx`). Verificado en navegador (HTTP 200, sin errores de compilación). Bug de controles en el vídeo confirmado no reproducible por el usuario. Triage de TODOs: `scrollSpeed` y fuentes custom ya estaban implementados. Nuevos TODO: tests con Vitest, resolución de export elegible. Errores de `tsc` preexistentes en `useVideoExport.ts`/`CreditPreview.tsx`/`examples` (no introducidos esta sesión).

- 2026-06-23: implementada pausa personalizada por item en modo appearing. Campo opcional `pauseOverride` en `CreditItem` (`types.ts`). Lógica de duración des-duplicada y extraída a módulo puro `src/lib/credit/appearing.ts` (`getAppearItemDuration` + `resolveItemPause`); `AppearingCredits.tsx` ahora la consume en vez de repetir el cálculo inline. UI: input "Pausa (s)" + badge en `CreditEditor.tsx`, visibles solo en modo appearing. Tests: nuevo `appearing.test.ts` (7) + caso roundtrip de `pauseOverride` en `store.test.ts`; 42 verdes. Eliminado el bloque `"pnpm"` inerte de `package.json`. Caché `.next` corrupta (TurbopackInternalError "Failed to write app endpoint", agravado por "Slow filesystem detected" en D:) borrada y dev server reiniciado limpio en :3000 (HTTP 200). Verificado en navegador por el usuario.

- 2026-06-23: ajustado el color del divider por item a selector real (swatch `<input type=color>` con color efectivo + hex + botón "global"), `CreditEditor.tsx`. Commit `98ecf37`. Brainstorming + spec + plan para dos features nuevas: (1) tipo de item `image`/logo (subida a data URL, render en scroll y aparición, ancho default global + override por item) y (2) override por item en textos (tamaño, color, fuente, interletraje, interlineado) vía resolver puro `resolveTextStyle` compartido por los tres puntos de estilo de texto. Spec en `docs/superpowers/specs/2026-06-23-logo-item-and-text-overrides-design.md` (6 huecos cerrados tras revisión: ADD_MENU_TYPES, TYPE_ICONS local, gating del panel, cortocircuito typewriter, tercer punto de estilo, fila resumen). Plan en `docs/superpowers/plans/2026-06-23-logo-item-and-text-overrides.md` (7 tareas TDD, suite objetivo 66, anclas de inserción verificadas contra el código). Commits `b36f580`, `9e349bf`, `db5f6b2` y plan. Pendiente: ejecutar el plan inline en la próxima sesión.

- 2026-06-23: ejecutado el plan de spacers/dividers configurables (6 tareas TDD inline). Nuevos campos opcionales por item en `CreditItem` (`spacerHeight`, `dividerThickness/Width/Opacity/Style/Color`) y defaults globales en `CreditConfig`; tipo `DividerStyle`. Módulo puro `separators.ts` (`resolveSpacerHeight`/`resolveDivider`, override >= 0 ?? global, color cae a `config.textColor`). `merge` custom en el persist de zustand vía `mergePersistedConfig` para backfill de claves nuevas en estados ya guardados. Render de divider migrado de `background` a `borderTop` (habilita dashed/dotted) en `ScrollCredits.tsx`. Eliminados los puntitos de progreso del modo aparición (`AppearingCredits.tsx`). UI: panel de edición por item de spacer/divider + filas atenuadas en aparición (`CreditEditor.tsx`); sliders/select/switch globales en la sección Diseño (`ConfigPanel.tsx`). Suite 42 -> 54 verdes (9 `separators.test.ts` + 3 `store.test.ts`). Commits `4104fd8`, `4ae1a4c`, `467dadb`, `f323dc6`, `d2c0e60`, `5958318`. Verificación interactiva en navegador pendiente del usuario.

- 2026-06-23: sesión de diseño (sin tocar código de la app). Cerrado el TODO de tiempos por item en modo scroll (descartado: en scroll el espaciado se logra con líneas en blanco, un timing por item sería redundante). Brainstorming → spec y plan para dos features: (1) spacer/divider configurables con default global + override por item (módulo puro `separators.ts` con `resolveSpacerHeight`/`resolveDivider`, `merge` en el `persist` de zustand para backfill de claves nuevas, render con `borderTop` para dashed/dotted, UI en `CreditEditor` y `ConfigPanel`); (2) retirada de los puntitos de progreso en modo aparición. Spec en `docs/superpowers/specs/2026-06-23-spacers-dividers-config-design.md`, plan en `docs/superpowers/plans/2026-06-23-spacers-dividers-config.md` (6 tareas TDD, suite objetivo 54 tests). Nuevos TODO registrados: override de fuente/tamaño/color por item, navegación fuera del frame en aparición, vignette sin control, opacidad de sombra, animaciones de aparición hardcodeadas. Commits `72b7011`, `27b8896`, `a75e2bc`, `d1dcea2`. Pendiente: ejecutar el plan inline en la próxima sesión.

- 2026-06-23: sesión de implementación inline (sin ejecutar el plan de logo/text-overrides, que sigue pendiente). Cinco TODO quirúrgicos: (1) borrado de `startDelay` muerto en `types.ts`; (2) control de bucle movido de `ConfigPanel` a la barra de reproducción de `CreditPreview` y, por petición del usuario, cableado también en modo scroll (antes el scroll ignoraba `config.loop` y siempre repetía) más botón visible en ambos modos; (3) opacidad de sombra (`textShadowOpacity` + helper puro `text-shadow.ts`); (4) vignette configurable (`vignetteEnabled`/`vignetteHeight`, unificado 20%); (5) tunables de animación de aparición (`getVariants` parametrizado). Tres TODO medianos: (6) drag-and-drop HTML5 nativo en `CreditEditor` (grip `<span draggable>` + `dataTransfer` + `reorderItems`); (7) blur de texto global + override por item (`text-blur.ts` `resolveTextBlur`, aplicado en scroll/aparición/typewriter); (8) velocidad de typewriter expuesta (`typewriterSpeed`, reemplaza el `50` hardcoded; causa raíz del efecto intermitente aún sin diagnosticar). Fixes: error de tipos preexistente en `ExportDialog.tsx` (prop `stageRef` a `RefObject<HTMLDivElement | null>`); bug del loop de scroll (el `setTimeout` de la pausa final se programaba dentro del updater por frame → ~180 timers peleando, el scroll se quedaba pegado arriba; ahora un único timer guardado + `clearTimeout` en el cleanup); retraso largo del loop (eliminado el div de padding inferior redundante en `ScrollCredits`, duplicaba un `containerHeight` de scroll vacío; `getScrollTotalDistance` ya añade esa pantalla). UX: tres `confirm()` nativos (Ejemplo, Vaciar lista, Reset) migrados a modales `AlertDialog`; nuevo botón "Ejemplo" que carga `DEFAULT_ITEMS`. Tests: nuevos `text-shadow.test.ts` (6), `getVariants.test.ts` (5), `text-blur.test.ts` (4); máquina de estado del loop de scroll extraída a `stepScrollProgress` en `scroll.ts` (función pura) con 9 tests de regresión, y `ScrollCredits` refactorizado para consumirla. Suite 42 → 78 verdes. `tsc`: cero errores nuevos; quedan 8 preexistentes (6 en `useVideoExport.ts` por tipos FFmpeg-wasm, 2 en `examples/websocket`). Verificación interactiva en navegador por el usuario (HTTP 200) de todo lo anterior.

- 2026-06-23: arreglados los 8 errores `tsc` preexistentes (3 ediciones en `useVideoExport.ts`, ver TODO) y borrado `examples/websocket/*`. Commit `1abee6e`. Verificación auditada del state contra la realidad: confirmado que el stack Prisma (`prisma/schema.prisma` con `User`/`Post`, `src/lib/db.ts`) no tenía ningún consumidor (`@/lib/db` no se importaba en `src`). Limpieza completa de scaffold z.ai (commit `b588727`, ~7900 líneas fuera): eliminado el stack Prisma entero (schema, `db.ts`, `.env`/`DATABASE_URL`, deps `prisma`+`@prisma/client`, scripts `db:*`, entradas en `pnpm-workspace.yaml`); 34 deps con cero imports en `src` (dnd-kit, next-auth, next-intl, mdxeditor, recharts, tanstack/*, react-markdown, react-syntax-highlighter, react-hook-form, hookform/resolvers, react-day-picker, date-fns, embla, vaul, input-otp, cmdk, zod, reactuses/core, bun-types y los radix de componentes ui no usados); 23 de 48 componentes `shadcn/ui` sin usar (se conservan los 24 que la app importa); `src/app/api/route.ts` ("Hello, world!" placeholder); scripts `build`/`start` simplificados (quitado `bun` + `tee` + `cp` de standalone → `next build`/`next start`). Verificado: `tsc --noEmit` limpio, 78 tests verdes, `next build` OK, dev HTTP 200. Por último, quitados de `next.config.ts` los parches z.ai `typescript.ignoreBuildErrors: true` y `output: "standalone"`; `next build` vuelve a ejecutar el type-check (antes lo saltaba) y pasa limpio. Commit `75a7d56`.

- 2026-06-23: sesión interrumpida (se cerró PowerShell) recuperada leyendo git log. El plan logo/imagen + text-overrides (`docs/superpowers/plans/2026-06-23-logo-item-and-text-overrides.md`) estaba a medias: Tasks 1-5 ya commiteadas en la sesión previa (`4f3c0fb`, `8a7cec9`, `b20f10e`, `65367ee`, `c0c138d`), Task 6 solo con el import `ImageIcon` y un error `tsc` (`TYPE_ICONS` sin la clave `image`). Completado inline: Task 6 (registro de `image` en `TYPE_ICONS`/`ADD_MENU_TYPES`, exclusión del panel de texto, fila resumen, panel de subida de logo en `CreditEditor`, slider de ancho global en `ConfigPanel`) commit `5018aea`; Task 7 (2 tests roundtrip de overrides de texto e image en `store.test.ts`) commit `1a507d6`. Suite 78→90. Verificado: `tsc` 0, `next build` con type-check OK, dev HTTP 200. Encontrado y cerrado un dev server huérfano de la sesión previa en :3000 (PID 13784, causaba EADDRINUSE).
- 2026-06-23: generado `creditos-demo.json` (72 items) a partir del texto del usuario, para importar con el botón Importar del editor (formato `{ items: [...] }`, que `handleImport` lee llamando `loadItems`; no requiere `config`). Mapeo: role=cargo, name=persona, title/subtitle, description para frases y conectores, spacer/divider; los 3 apoyos con logo van como item `name` + item `image` vacío (subir el PNG en la app).
- 2026-06-23: bug de fuentes custom diagnosticado con systematic-debugging. Síntoma: subir fuente OK pero tras recargar no se mostraba (caía a la última fuente de la lista). Causa raíz: el `@font-face` de las custom solo se inyectaba al subir (`handleFileUpload`); al recargar, `FontLoader` reinyectaba solo las Google (`source === "google"`), y las custom (persistidas con su data URL en `font.url`) quedaban sin regla `@font-face`. Fix: helpers puros en `fonts.ts` (`buildFontFaceRule`, `fontFormatFromDataUrl`, `fontFaceStyleId`), `FontLoader` reinyecta el `@font-face` de las custom al cargar, y el `url()` del data URL ahora va entre comillas. 5 tests nuevos en `fonts.test.ts`. Suite 90→95. Verificado en navegador por el usuario (la fuente custom se ve tras recargar). Además conservado un fix de sesión previa sin commitear en `scroll-area.tsx` (viewport Radix: `[&>div]:!block [&>div]:!w-full`, fuerza el contenido al ancho completo).

- 2026-06-23: auditoría de TODOs contra el código real (pedida por el usuario): el override por item de fuente/tamaño/color estaba implementado pero su TODO seguía marcado `[ ]` (quedó sin actualizar tras el plan logo/text-overrides); corregido a `[x]`. Verificados como NO implementados (siguen pendientes correctamente): presets propios, control de fade, márgenes seguros, flash de hidratación. Launcher del dev server: `dev.ps1` en la raíz (usa `$PSScriptRoot`, `pnpm dev` → :3000) + registrado en ACTA como `[11] "Credits app dev"` (invoca el `.ps1`). Implementado el fix del flash de hidratación con TDD (`_hasHydrated` + `onRehydrateStorage` en `store.ts`, gate de render en `page.tsx`). Suite 95→96 verdes, `tsc` limpio. Verificado en navegador por el usuario.

- 2026-06-24: estudiada "sequentia" (proyecto propio, app vanilla single-file serverless) para aprender de su exportación rápida. Hallazgo: su motor es **WebCodecs `VideoEncoder` (H.264 por hardware) + Mediabunny** renderizando frames a `OffscreenCanvas` en un Web Worker, sin captura PNG por frame ni FFmpeg-wasm; mide el tiempo real con `performance.now()`; estima el tamaño por bitrate `(bitrate/8)*segundos`. Volcados estos aprendizajes en el TODO de "exportación lenta". Auditada la carpeta `download/` (resto huérfano del scaffold z.ai, gitignored, sin uso en `src`). Añadidos 5 TODO nuevos pedidos por el usuario (sección "Exportación: gestión de archivos y feedback"): vídeos generados siempre accesibles, cronómetro de exportación en vivo, auditoría del export de proyecto JSON, fix del tamaño aproximado mal calculado (135 MB vs 7,6 MB reales), y elegir destino de guardado (File System Access API en vez de descarga automática a Descargas). Sesión solo de investigación + documentación; sin cambios en código de la app.

- 2026-06-24: mejoras quirúrgicas del modal de exportación (sin spec/plan formal por decisión del usuario, cambios acotados). (1) Cronómetro de exportación en vivo + ETA: módulo puro `exportTiming.ts` (`formatElapsed`, `estimateRemainingSeconds`, 11 tests) consumido por `ExportDialog`; fila "transcurrido · restante ~ETA" y total en el cuadro final. (2) Quitada la estimación de tamaño previa (engañaba con CRF + contenido comprimible: 135 MB vs 7,6 MB reales); grid de resumen 2×2 → 3 cols; se mantiene el tamaño real al terminar. (3) Elegir destino de guardado vía File System Access API (`showSaveFilePicker` al iniciar, escritura con `createWritable` al terminar; cancelar aborta; fallback a descarga). `triggerDownload` extraído y reutilizado en "Descargar de nuevo". Suite 96→107 verde, `tsc` limpio. Pendiente verificación interactiva en navegador por el usuario (especialmente el selector de carpeta, que solo funciona en Chrome/Edge). Quedan como TODO: galería/historial persistente de vídeos y la reescritura del motor a WebCodecs+canvas.

- 2026-06-24: segunda tanda de cambios acotados. (1) Consolidado el I/O de proyecto: quitado el export/import redundante de solo-items del `CreditEditor` (handlers + fileInput + botones); el export completo de proyecto ya vivía en `CreditPreview` (`exportProject` del store, que serializa config+items+fonts+projectName, completo por construcción). El usuario confirmó que no necesita compatibilidad con JSON antiguos. (2) Márgenes seguros: overlay no exportable + toggle (ver TODO). (3) Resolución de exportación: multiplicador de escala vía `pixelRatio` + filtro de dimensiones pares (ver TODO). (4) Overrides por item: animación (type/duration/tunables) + peso de fuente, con resolvers puros y UI por item; auditados el resto de globales (ver TODO de overrides por item y el de fade). Suite 96→117 verde, `tsc` limpio en cada paso. Diagnosticado y matado un dev server zombi (PID 4980) que retenía :3000 sin responder (causaba EADDRINUSE al relanzar); nuevo dev server limpio. Errores `getFontWeight/tunablesFromConfig is not defined` en consola fueron estados intermedios de HMR durante la edición incremental; el build final compila limpio. Verificado en navegador por el usuario ("funciona todo").

- 2026-06-24: tres features con spec+plan (`docs/superpowers/specs|plans/2026-06-24-timeline-wrap-safe-margins*`) ejecutadas inline en `main` (TDD por tarea, 11 tareas). (1) Línea de tiempo: `TimelineBar.tsx` scrubbeable reutilizando `manualProgress`; reporte de progreso a un `ref` + RAF propio de la barra (cero re-render de escenarios por frame); seed-on-resume en `ScrollCredits`; saltos por item (botones + "X/Y") y muescas por item en aparición; módulos puros `safeMargins.ts` (`resolveSafeInset`) y `timeline.ts` (`itemProgressBounds`). (2) Wrap: `noWrap` + `textBoxWidth` global y por item vía `resolveTextStyle` ampliado (`whiteSpace`/`wordBreak`/`maxWidth`), en scroll/aparición/typewriter; UI en `ConfigPanel` (Tipografía) y `CreditEditor`. (3) Respetar márgenes: `respectSafeMargins` constriñe el contenido a la caja título-segura (inset horizontal 10% dentro de los componentes → afecta al export); botón "Respetar" + guía renombrada a "Guía". Extra pedido por el usuario: tecla espacio = play/pause (`keyboard.ts` `isInteractiveTarget`); refactor UI con sección "Efectos" nueva (sombra/viñeta/blur fuera de "Colores y fondo"). Auditado el export de proyecto JSON: completo por construcción (verificado con roundtrip). Suite 117→135 verde, `tsc` limpio en cada tarea. Verificado en navegador por el usuario ("funciona todo"). Nuevos TODO de mejoras de timeline (tooltip al hover, resalte del item activo, doble clic en item → seek).

- 2026-06-24: implementada la feature "presets propios" (brainstorming → spec → plan → ejecución inline en `main`, 2 tareas TDD). Spec `docs/superpowers/specs/2026-06-24-user-presets-design.md`, plan `docs/superpowers/plans/2026-06-24-user-presets.md` (revisados huecos en ambos: footgun de `Dialog` dentro de `DropdownMenu` resuelto controlando el open con `useState` fuera del menú; borrado con botón `X` que hace `preventDefault`+`stopPropagation`; duplicados bloqueados; test de persistencia retirado del spec por no idiomático aquí, sustituido por verificación manual). Task 1 (`e96510a`): tipo `UserPreset`, slice `userPresets` + 3 acciones + persistencia (`partialize`/`merge` con backfill), 5 tests (suite 135→140). Task 2 (`91d8941`): dropdown "Mis presets" en `PresetBar`. `tsc` limpio, `next build` OK. Verificado en navegador por el usuario (guardar/aplicar/borrar/duplicado/persistencia tras recarga/ausencia en export JSON). Pendientes hermanos no abordados esta sesión: override por item de `typewriterSpeed` y de sombra de texto.

- 2026-06-25: cerrado el TODO de "navegación fuera del frame" como superseded por el `TimelineBar` (commit `36f5c8f`). Implementados los overrides por item pendientes (brainstorming + diseño aprobado, sin spec/plan formal por decisión del usuario; TDD inline en `main`, commit `9640537`). (1) Sombra de texto por item: 6 campos nuevos en `CreditItem` (`useTextShadow` tri-estado undefined/true/false, `textShadowColor/Blur/X/Y/Opacity`); `resolveTextShadow` pasa de `(config)` a `(item, config)` con fallback por campo (color por `||`, numéricos por nullish admitiendo 0 y negativos en X/Y); actualizados los 2 call sites (`ScrollCredits`, `AppearingCredits`); subcomponente `ShadowOverrides` en `CreditEditor` (Select tri-estado + 5 campos visibles solo cuando la sombra queda efectiva), en ambos modos, solo items de texto. (2) `typewriterSpeed` por item: campo `item.typewriterSpeed`, resolver `resolveTypewriterSpeed(item, config)` (override >0 ?? global); usado en los dos `useEffect` de tecleo de `AppearingCredits` y, de paso, en `getAppearItemDuration` (antes la duración del item usaba `0.05` hardcodeado en vez de la velocidad real: bug latente corregido, coincidía con el default 50ms); input en `AnimationOverrides` solo cuando el tipo efectivo es typewriter. Tests: +11 (text-shadow.test.ts firma nueva + overrides, appearing.test.ts resolveTypewriterSpeed, store.test.ts roundtrip de los campos nuevos). Suite 140→151, `tsc` limpio, `next build` OK. Verificado en navegador por el usuario. SIGUE pendiente el bug "typewriter solo teclea el primer item" (otro TODO, no tocado aquí).

- 2026-06-25: sesión de diseño (sin tocar código de la app). Brainstorming → spec → plan para el refactor "inspector contextual + lista pura". Spec en `docs/superpowers/specs/2026-06-25-inspector-panel-and-list-refactor-design.md` (commits `33f1000` + `aea003d` cierre de huecos), plan en `docs/superpowers/plans/2026-06-25-inspector-panel-and-list-refactor.md` (8 tareas TDD; commits `8005058` + `b9c2603` cierre de huecos). Contenido: (1) panel izquierdo a lista pura (crear/reordenar/seleccionar, sin edición inline); (2) panel derecho a inspector contextual (config global sin selección; propiedades del item por tipo, con default "global"; toggle "Ver global"; clic de nuevo en la fila seleccionada deselecciona); (3) reorg de la config global (Escenario / Modo / Tipografía / Colores / Efectos / Diseño / Separadores / Imágenes / Animación); (4) inserción "+" entre filas reutilizando `addItem(type,_,index)`; (5) doble clic en fila → seek de la línea de tiempo (aparición vía `seekToItem`; scroll vía geometría real medida por `ScrollCredits`); (6) resalte del item activo en la lista, sincronizado con reproducción y con el desplazador. Helpers puros nuevos previstos en `scroll.ts` (`scrollProgressForItem`, `activeItemIndexAtProgress`); canales efímeros en store (`seekTarget`, `activeItemId`, no persistidos); fix del placeholder vacío de `addItem`. Decisiones: regresión de entrada de texto aceptada; el resalte inverso (timeline→lista) y el doble-clic-seek de los TODO de timeline quedan cubiertos por el plan. Huecos cerrados tras dos auditorías (spec y plan): reutilizar `addItem` en vez de un `insertItem` nuevo; geometría real de scroll para seek y resalte (sustituye el seek lineal); resalte sincronizado con scrub (`manualSeekRef`, porque `onProgressChange` no emite en modo manual); deselección por toggle; cabecera única en `InspectorPanel`; selector booleano del resalte; filtrado `height>0` de offsets. Pendiente: ejecutar el plan INLINE en la próxima sesión.

- 2026-06-25: ejecutado INLINE en `main` el plan "inspector contextual + lista pura" (`docs/superpowers/plans/2026-06-25-inspector-panel-and-list-refactor.md`), 8 tareas TDD, un commit por tarea (`fa169ca`, `19fc938`, `aafcaa5`, `a7a32fa`, `8b287fb`, `8a55829`, `3e3a831`, `d18811b`). (1) Store: canales efímeros `seekTarget`/`activeItemId` (no persistidos) + acciones `requestSeek`/`setActiveItem`; fix del placeholder de `addItem` (con `text=""` por defecto el `?? placeholder` nunca aplicaba, insertaba texto vacío). (2) `scroll.ts`: helpers puros `scrollProgressForItem` y `activeItemIndexAtProgress`. (3) Config global reorganizada en secciones Escenario / Modo / Tipografía / Colores / Efectos / Diseño / Separadores / Imágenes / Animación (extraídas Escenario, Separadores e Imágenes). (4) `ConfigPanel` partido en `InspectorPanel` (cabecera única + toggle Ver global/item) + `GlobalConfig`; nuevo `ItemInspector.tsx` con los controles por item (movidos `AnimationOverrides`/`ShadowOverrides` y los paneles texto/spacer/divider/image desde `CreditEditor`); `ItemRow` adelgazado a lista pura; `page.tsx` cableado a `InspectorPanel`. (5) Inserción "+" entre filas (`InsertGap`) reutilizando `addItem(type,_,index)`; `onSelect` togglea (deselección). (6) `ScrollCredits`: `CreditLine` a `forwardRef`, prop `onLayoutChange` que reporta offsets por item (filtrados `height>0`) + alturas. (7) doble clic en fila → `requestSeek`; `CreditPreview` observa `seekTarget` y hace seek (aparición `seekToItem`, scroll `scrollProgressForItem` sobre geometría real). (8) resalte ámbar del item activo en la lista (selector booleano por fila), producido en aparición por `currentItemIndex` y en scroll por un RAF propio sobre `activeItemIndexAtProgress` (con `manualSeekRef` porque `onProgressChange` no emite durante el scrub). Tests +10 (store +5, scroll +5); suite 151→161. `tsc` limpio y `next build` OK en la verificación final. Verificado en navegador por el usuario ("funciona todo perfectamente"). Cerrados de paso dos TODO de "Timeline: mejoras" (resalte del item activo y doble-clic-seek). Regresión aceptada: la entrada de texto del item ahora vive solo en el inspector (mitigable con el TODO de importar CSV/TXT).

- 2026-06-26: añadida capacidad de **export estático** a la app (es 100% cliente, sin server features). `next.config.ts` gana dos modos gateados por env var, sin afectar a `pnpm dev` ni al build normal: `PORTABLE=true` → export SIN `basePath`, servible en la raíz con cualquier servidor estático; `build-portable.ps1` lo buildea y empaqueta `credits-portable.zip` (+ `HOW-TO-RUN.txt`) para llevar a otro PC y correr con `npx serve .` / `python -m http.server`. `EXPORT=true` → export bajo un `basePath`, para servirlo detrás de un prefijo de ruta. `out/` se genera con `tsc` y `next build` limpios; verificado con prueba de humo (index 200, assets 200). Caveat: `showSaveFilePicker` (elegir carpeta al exportar) requiere contexto seguro; sobre HTTP no seguro cae al fallback de descarga.

- 2026-06-30: feature "línea a línea" en modo aparición (brainstorming → implementación inline; luego, a petición del usuario, rediseñada de tipo a modificador combinable). Modelo final: `staggerLines` (global en `CreditConfig`, default `false`) + override tri-estado por item (`CreditItem.staggerLines?`), y `lineRevealInterval` (global default `0.4`s) + override por item; cuando está activo, el texto del item se parte por `\n` y cada línea entra con el tipo de animación elegido (fade/desenfoque/slide/zoom), escalonada por el intervalo, combinable. `appearing.ts`: `resolveStaggerLines`, `resolveLineRevealInterval`, `countItemLines`, `isLineStaggered` (flag on + no typewriter + >1 línea) y rama de `getAppearItemDuration` (`(n-1)*interval + animDuration + pause`). `AppearingCredits.tsx`: `LinesItem` (cada línea con las `variants` del tipo). `getVariants` gana caso `default` (cae a fade) que arregla un crash al quedar `animationType:"lines"` heredado en localStorage de una iteración previa. UI: toggle + slider global en `ConfigPanel` (no en typewriter), tri-estado + input por item en `ItemInspector`. Tests +14 (`appearing.test.ts`: `resolveStaggerLines`, `isLineStaggered`, `getAppearItemDuration` line-by-line, `resolveLineRevealInterval`; `store.test.ts`: roundtrip de las claves nuevas). Suite 161→175, `tsc` limpio. **Bug pendiente (no resuelto esta sesión)**: el revelado por intervalo (línea a línea Y máquina de escribir) no es fiable — a veces escalona, a veces sale el bloque entero; no determinista. CAUSA RAÍZ diagnosticada con systematic-debugging: typewriter y líneas son las únicas animaciones que avanzan con `setInterval` en un efecto del padre disparado por `currentIndex`; `<AnimatePresence mode="wait">` retrasa el montaje del item nuevo hasta que acaba la salida del anterior, así que el contador corre durante ese hueco y el item aparece ya (parcial o totalmente) revelado. Encaja con el bug histórico del typewriter ("solo teclea el primer item"): el primer item no espera salida. Verificado por logs de consola (con item de 5 líneas que monta limpio, el contador sube 2→3→4→5). Spec en `docs/superpowers/specs/2026-06-30-line-by-line-reveal-and-interval-mount-timing-design.md` y plan en `docs/superpowers/plans/2026-06-30-line-by-line-reveal-and-interval-mount-timing.md` (6 tareas TDD; revisado y cerrados 5 huecos: compilación entre tareas, asunción de `mode="wait"` con verificación + plan B vía `onAnimationStart`, conteo de tests, ternario, deps de `MountSignal`). Diseño del fix: el revelado pasa a los hijos (`LinesItem`/`TypewriterItem`) que se montan al hacerse visible el item; avance anclado a una señal de montaje (`shownTick`/`MountSignal`); clave por aparición (`appearanceKey` con `cycle`) para loop/reinicio. **Pendiente: ejecutar el plan INLINE en la próxima sesión.** Errores de consola de Google Fonts (`NS_ERROR_CORRUPTED_CONTENT`/MIME) constatados como previos y ajenos (URLs mal construidas por `FontLoader` con comillas + fallback); pertenecen al TODO de privacidad/fuentes.

- 2026-06-30: ejecutado INLINE en `main` el plan `docs/superpowers/plans/2026-06-30-line-by-line-reveal-and-interval-mount-timing.md` (6 tareas TDD, un commit por tarea). Arregla el revelado no determinista por intervalo (línea a línea y máquina de escribir) en modo aparición. (1) `appearing.ts`: helpers puros `revealedLinesAt`/`typedCharsAt` (Task 1, `8834dae`). (2) El efecto `manual-derive` de `AppearingCredits` los consume y los estados pasan a `manualTypedText`/`manualRevealedLines` (solo modo export/scrub); borrados los dos efectos de intervalo en vivo del padre (Task 2, `df3684b`). (3) `LinesItem` deja de recibir el contador del padre y gestiona su propio revelado en vivo con un `setInterval` que arranca al montarse el componente (Task 3, `e8b5d2f`). (4) Render inline del typewriter extraído a `TypewriterItem`, dueño de su tecleo en vivo, simétrico (Task 4, `9b3cebb`). (5) Motor del padre re-anclado a la visibilidad (Task 5, `2c58685`): clave del `motion.div` por aparición (`${id}-${currentIndex}-${cycle}-${restartKey}`), `MountSignal` (efecto `[]` que dispara `handleShown` al montar; con `mode="wait"` el nodo entrante se monta solo tras la salida del anterior, así que es la señal fiable de "ahora visible", válida también para el primer item), y un único efecto de avance disparado por `shownTick` que lee `currentIndex` por ref y reprograma `setTimeout(getAppearItemDuration)` (incl. `cycle++` en el reset de loop para que el loop de un solo item re-monte y rearranque su entrada). CAUSA RAÍZ (era): typewriter y líneas eran las únicas animaciones por `setInterval` en un efecto del padre disparado por `currentIndex`, que corría durante el hueco de salida de `mode="wait"`; ahora revelado y avance se anclan al montaje del hijo visible. Suite 175→182 (7 tests nuevos de los helpers puros), `tsc` limpio, `next build` OK. Verificado en navegador por el usuario ("va perfecto"): escalonado en todos los items multilínea, tecleo en todos los items, loop de un solo item rearranca, doble clic muestra el item, resto de animaciones bien. La asunción load-bearing de `mode="wait"` quedó confirmada por esa verificación; no hizo falta el plan B. (No pude correr la comprobación con `console.log` en navegador desde la sesión: el Chrome del usuario daba `chrome-error` en `localhost`/`127.0.0.1` pese a HTTP 200 por curl, problema de red/proxy ajeno al código.) Tras verificar, regenerado el paquete portable con `build-portable.ps1` (`credits-portable.zip` + `credits-portable/`, gitignored) para que incluya este fix.

- 2026-07-02: auditoría completa del código (pedida por el usuario) con `tsc` limpio y 182 tests verdes como base. Bugs encontrados: (1) Google Fonts no cargaban tras recargar — `FontLoader` y el efecto del diálogo de `FontManager` pasaban `f.family` (valor CSS `'Inter', sans-serif`) a `buildGoogleFontUrl`, que espera el nombre plano → URL malformada → error de Google; era el origen real de los errores de consola `NS_ERROR_CORRUPTED_CONTENT` atribuidos antes al TODO de privacidad. ARREGLADO: pasar `f.name` (2 call sites, `FontManager.tsx`). (2) `useMemo` de `itemDurations` en `AppearingCredits.tsx` sin `config.staggerLines` en deps → togglear "Revelar línea a línea" no recalculaba duraciones/timeline/duración de export. ARREGLADO: dep añadida. Verificación tras ambos fixes: `tsc` limpio, 182/182 verdes. Hallazgos NO arreglados registrados como TODO nuevos (sección "Auditoría 2026-07-02"): export no determinista de animaciones en aparición, Cancelar inerte durante encoding, eslint capado por config z.ai, archivos/config muertos, `SYSTEM_FONTS` a medias, validación de `importProject`, menores.

- 2026-07-02: ejecutados los 8 fixes pendientes de la auditoría (detalle y commits en los TODO de la sección "Auditoría 2026-07-02", todos marcados `[x]`). Orden: limpieza mecánica (archivos muertos `1433e7f`, config muerta `fd25930`, menores `d38f36f`), validación de `importProject` con TDD (`dbf0eb6`), Cancelar durante encoding (`c1c5002`), fuentes de sistema completadas con TDD (`344717c`), eslint restaurado + limpieza aflorada (`9be48cc`) y export determinista de aparición con TDD (`9f4d889`). Suite 182→196 verdes; `tsc`, `pnpm lint` y `next build` limpios; dev server HTTP 200 (cerrado con Stop-Process). Pendiente verificación interactiva del usuario: MP4 de aparición (entradas suaves, sin salidas), Cancelar durante encoding, pestaña "Sistema" de fuentes, scrub del timeline.

- 2026-07-03: sesión de diseño de la feature "item de texto rico" (sin tocar código de la app). Brainstorming → decisiones: formato por fragmento (fuente, tamaño, negrita booleana sobre `fontWeight` global, cursiva, color); los tipos title/subtitle/name/role/description se ELIMINAN (queda `text` + spacer/divider/image, sin presets de formato); editor en el inspector; TipTap v3 elegido tras verificar alternativas 2026 (Lexical/Slate/Plate) y confirmar que `TextStyleKit` de `@tiptap/extension-text-style` trae FontFamily/FontSize/Color oficiales; modelo propio `Rich` (líneas → runs) con conversores puros en la frontera del editor; sin migración legacy (app en test, hard reset de localStorage). Spec en `docs/superpowers/specs/2026-07-03-rich-text-item-design.md` (commits `718034b`, `d83175f` con 8 huecos cerrados: text derivado en `updateItem`, `\n` en `sliceRuns`, validación de `rich` en import, nbsp por línea vacía, `plainToRich` compartido, `fontSize` único default 36, negrita booleana, borrado de fuente bloqueado por `fontInUseByItems`). Plan en `docs/superpowers/plans/2026-07-03-rich-text-item.md` (9 tareas TDD, estrategia aditiva: el tipo rico convive con los viejos hasta la tarea 8; commits `1bb43d8`, `dc0986a` con 4 huecos cerrados: seed del editor con `plainToRich`, icono `TypeIcon` reutilizado, test del placeholder de `addItem`, nota del JSON de ejemplo legacy). Pendiente: ejecutar el plan INLINE en la próxima sesión.

- 2026-07-03: ejecutado INLINE en `main` el plan `docs/superpowers/plans/2026-07-03-rich-text-item.md` (8 tareas de código TDD, un commit por tarea). (1) Modelo `Rich` (líneas → runs con `RichStyle`: fontFamily/fontSize/bold/italic/color) en `types.ts` + helpers puros `rich.ts` (`plainToRich`, `richToPlain`, `sliceRuns`, `isValidRich`, `fontInUseByItems`), commit `dbb120e`. (2) TipTap v3 (3.27.1) instalado + conversores puros `tiptapRich.ts` (`tiptapToRich`/`richToTiptap`), commit `9dfc340`. (3) Tipo `text` aditivo con placeholder "Nuevo texto" y sync de texto derivado en `updateItem` (rich → `richToPlain`), commit `51cb7a6`. (4) `RichText.tsx` (`RichText`/`LineSpans`, runs como spans con solo las props overrideadas) cableado en ambas escenas, commit `f7b99cb`. (5) Typewriter por contador de chars + `sliceRuns` (prop `manualTypedChars`) y `LinesItem` sobre líneas ricas, commit `350416f`. (6) `RichTextEditor.tsx` (TipTap: Document/Paragraph/Text/Bold/Italic/TextStyleKit/UndoRedo, toolbar fuente/tamaño/B/I/color/quitar formato, pegado como texto plano, setContent solo al cambiar de item) en el inspector para `type === "text"`, commit `a072a5b`. (7) `isValidImportedItems` valida `rich` + borrado de fuente bloqueado si la usa algún run (`FontManager`), commit `db4b0de`. (8) Barrido legacy, commit `05699ac`: `CreditItemType` = text/spacer/divider/image; fuera `bold`/`italic`/`fontSize`/`color`/`fontFamily`/`fontWeight` por item, los 5 tamaños por tipo (→ `config.fontSize` único, default 36, slider "Tamaño base"), `getFontSize`/`getFontWeight`/`resolveFontWeight` (escenas usan `config.fontWeight`), ramas Input/Textarea y botones B/I del inspector, badges B/I de la lista; `DEFAULT_ITEMS` regenerado como demo rica (16 items); presets de `PresetBar` con `fontSize`. Suite 196→207 (`rich.test.ts` 12, `tiptapRich.test.ts` 5, store +5; -11 de describes legacy borrados), `tsc`/`pnpm lint`/`next build` limpios, dev HTTP 200 (cerrado, :3000 liberado). Pendiente verificación interactiva del usuario (editor con formato por selección, render en ambos modos, typewriter/stagger con runs, import/export). `creditos-demo.json` ya NO importa (tipos legacy); regenerarlo como items ricos es un TODO aparte si se quiere conservar.

- 2026-07-03: despliegue paralelo en la VM para verificación (detalle homelab en `~/.homelab/state.md`): `next.config.ts` gana `EXPORT_BASE` (default `/credits`, commit `3d79f00`) y `store.ts` gana `NEXT_PUBLIC_STORE_KEY` para la clave del persist (default `credit-titles-store`, commit `8524677`) — sin la segunda, dos deploys bajo el mismo origen comparten localStorage y el estado legacy de la versión vieja crasheaba la nueva (icono `undefined` por tipos de item eliminados; diagnosticado porque en incógnito cargaba). `/credits-rich/` desplegada con `EXPORT=true EXPORT_BASE=/credits-rich NEXT_PUBLIC_STORE_KEY=credit-titles-store-rich`. Suite 207 verde, `tsc`/`lint` limpios.

- 2026-07-03: ajustes de UI pedidos por el usuario (commit `9297e57`): (1) los 6 presets integrados del header pasan de botones sueltos (rompían el responsive) a un único dropdown "Presets" que integra también la sección "Mis presets" y "Guardar actual…"; (2) cabecera del inspector con toggle Item/Global (`ToggleGroup`) siempre visible — "Item" deshabilitado sin selección, Reset como botón de icono solo en vista global. Redesplegado en `/credits-rich/` y verificado en navegador por el usuario ("está perfecto").
- 2026-07-03 (cierre): feature de texto rico VERIFICADA por el usuario en `/credits-rich/` ("funciona todo") — queda cerrada la verificación pendiente de la ejecución del plan.
- 2026-07-03: flujo portable cambiado por decisión del usuario — el zip se retira; la versión portable ahora vive en una carpeta sincronizada entre PCs. `build-portable.ps1` reescrito: deploya `out/` + `HOW-TO-RUN.txt` directo a la carpeta sincronizada `credits-portable-rich` (nombre nuevo para no pisar `credits-portable`, la versión antigua pre-texto-rico que se conserva junto a `credits-portable_old`), sin paso de zip. Último zip archivado en `.backups/credits-portable-2026-07-03.zip`; carpeta local `credits-portable/` eliminada (regenerable). Ejecutado y verificado (index con rutas raíz + guía presentes). Redesplegar a otros PCs = `build-portable.ps1` y esperar la sincronización.

- 2026-07-04: sesión de diseño del motor de exportación (sin tocar código de la app). Reinvestigado el método de export desde cero a petición del usuario (no se fiaba de las conclusiones de sequentia): el TODO de "exportación lenta" reescrito con los hallazgos (ver TODO). Decisiones aprobadas: solo WebCodecs (se elimina la ruta FFmpeg-wasm y unpkg), captura html-to-image `toCanvas` (snapDOM aplazado a spike posterior si la captura sigue siendo cuello), solo MP4/H.264, `BufferTarget` (flujo de guardado intacto). Brainstorming → spec `docs/superpowers/specs/2026-07-04-webcodecs-export-engine-design.md` (commit `9474d62`; +7 huecos cerrados en `feab471`: staging canvas por el contrato un-solo-canvas de `CanvasSource` + dims pares que garantizaba el filtro ffmpeg, `await` de backpressure en `add()`, `output.cancel()`, `fastStart`, check de soporte al abrir el diálogo, rework de la ETA por fases de `ExportDialog`, barrido webm con filename/accept) → plan `docs/superpowers/plans/2026-07-04-webcodecs-export-engine.md` (commit `3103891`, 5 tareas: instalar mediabunny, helpers puros `exportSettings.ts` TDD suite 207→216, reescritura del motor con union transicional de fases para compilar entre tareas, rework del diálogo, limpieza tipos+deps; +3 huecos cerrados en `06c363b`: cancel entre bucle y finalize, `evenDimensions` compartido diálogo/motor, expectativa del grep de restos). Requisito de primera clase en el spec: los 3 orígenes de fuente (Google/custom/sistema) deben sobrevivir a la captura; posible hueco preexistente detectado (con Google Font activa, `skipFonts: true` podría dejar fuera las custom) — a confirmar en la verificación manual. **Pendiente: ejecutar el plan INLINE en la próxima sesión.**

- 2026-09-13: limpieza previa a la publicación y nombre definitivo TITULUS. Carpeta del proyecto renombrada y el repo subido de `app/` a la raíz del proyecto. Nada borrado, todo a `.archive/` (ignorado): `.tar` y backup git de z.ai (`zai/`); `.zscripts/`, `Caddyfile`, `mini-services/`, `bun.lock`, `download/`, `scripts/`, `db/` (`zai-scaffold/`, sacados del índice con `git rm --cached`); `build-portable.ps1` (`portable/`); `tailwind.config.ts` (`unused/`: con Tailwind 4 nadie lo leía, `components.json` apunta `config: ""`); `creditos-demo.json` y el contenido de `.backups/` (`data/`: el JSON lleva nombres reales de terceros y no es parte de la app; sigue en el historial en los commits `23dfa6c` y `fcd085f`). Retirados el flujo portable y el despliegue en la VM (eran para pruebas de viaje, ya no hacen falta): `next.config.ts` con un único modo `EXPORT=true` y `basePath` `/titulus`; `store.ts` con clave fija `titulus-store` (fuera `NEXT_PUBLIC_STORE_KEY`); `tailwindcss-animate` eliminado de `package.json` (solo lo referenciaba el config archivado); `.gitignore` reescrito. Nombre en `package.json`, `layout.tsx` (title y authors) y cabecera de `page.tsx`. README reescrito. Verificado: `tsc`, `pnpm lint`, 207 tests y `EXPORT=true pnpm build` limpios. Commits `67bbf2e`, `9745c27`. Decisión: el motor WebCodecs queda como mejora futura; se publica con FFmpeg-wasm. Pendiente: mover a `2_finished`, crear el repo y desplegar en Pages.
- 2026-09-13 (cierre): proyecto movido a `2_finished` y repo publicado en `memoriainfinita/titulus` (push de los 153 commits hechos por el usuario, con el historial tal cual: decidido no reescribirlo). Segunda pasada de archivado a `.archive/unused/`: `textarea.tsx` (nadie lo importaba), `public/logo.svg` (sin referencia) y `public/robots.txt` (lista de bots de z.ai). `toggle.tsx` se queda: lo usa `toggle-group`. Pages activado con `build_type=workflow` via API y workflow `.github/workflows/pages.yml`; primer despliegue OK. Verificado: `tsc`, lint, 207 tests. Commit `817c476`. Al mover la carpeta, pnpm reinstala `node_modules` completo desde la store (sin descargas) la primera vez que se ejecuta algo; en `D:` tarda varios minutos.
- 2026-09-13: añadido motor de exportación rápido (WebCodecs + Mediabunny 1.56.2) CONSERVANDO FFmpeg, por decisión del usuario (el spec/plan de 2026-07-04 lo eliminaba; no se reescriben). Selector "Motor" visible en el diálogo (Rápido por defecto si hay soporte; WebM fuerza FFmpeg). Archivos nuevos: `webcodecsExport.ts`, `exportSettings.ts` (+9 tests), `fontSubset.ts` (+13 tests), `fontEmbed.ts` (movido tal cual de `useVideoExport.ts`). API de Mediabunny comprobada en sus tipos: `QUALITY_*` obsoletas, se usa `new Quality('medium'|'high'|'very-high')`.
  - Medición inicial: el motor nuevo iba igual de lento que FFmpeg (~1 frame/s en dev y en build de producción). Causa confirmada por instrumentación: SVG de 10,47 MB por frame por las fuentes incrustadas, ~670 ms de carga. Solución solo en el motor rápido: subconjunto de `@font-face` por frame (familia/peso/estilo usados + `unicode-range` del texto visible; si falta el peso o el estilo exacto se incluye la familia entera), memoizado mientras no cambie.
  - Verificado en build estático servido bajo `/titulus/`, Chrome headless: proyecto de ejemplo (38 s, 1145 frames, 1280×720, 30 fps) en 4:15 con Rápido frente a 19:25 con FFmpeg; MP4 H.264 High yuv420p con los mismos frames y duración; SSIM 0,993-0,998 entre ambos. Fuente subida + Playfair Display (Google) en scroll y en aparición: correctas. Cancelar a mitad y volver a exportar: correcto. WebM desactiva Rápido. `tsc`, eslint y 229 tests verdes.
  - Commits `e031f53` (código) y `33b40c6` (docs).
  - Después, a petición del usuario ("arregla todo"): la selección de fuentes por frame pasa a `fontEmbed.ts` (`createFrameFontEmbedder`) y la usa también la ruta FFmpeg. Arregla las fuentes subidas en FFmpeg y la acelera (proyecto de 22 s: 616 s → 178 s). Verificado: fuente subida + Playfair con FFmpeg (scroll) y con Rápido (aparición); `tsc`, eslint, 229 tests.

## TODO

### Texto rico (2026-07-03)
- [x] Ejecutar INLINE el plan `docs/superpowers/plans/2026-07-03-rich-text-item.md`. Hecho 2026-07-03 (ver History): 8 tareas TDD, commits `dbb120e`→`05699ac`, suite 207 verde. Verificado en navegador por el usuario ("funciona todo", vía `/credits-rich/` en la VM).
- [x] Regenerar `creditos-demo.json` como items ricos. Hecho 2026-07-03: los 5 tipos legacy convertidos a `text`+`rich` (mapeo de la jerarquía vieja: title 72 bold, subtitle 42, name global, role 24, description 20; sin colores/cursivas inventados); spacer/divider/image intactos (72 items). Envuelto en `{ version, projectName, items, config: {} }` porque el botón Importar usa `importProject` desde 2026-06-24, que exige `config` objeto (la nota de 2026-06-23 "no requiere config" quedó obsoleta). Verificado con test one-off contra `importProject` real (72 items, OK; test borrado tras pasar). Original archivado en `.backups/creditos-demo-legacy-2026-07-03.json` (`.backups/` añadido a `.gitignore`). Paquete portable regenerado con `build-portable.ps1` (incluye el feature de texto rico).

### Auditoría 2026-07-02 (hallazgos pendientes)
- [x] Export en modo aparición no determinista. Resuelto 2026-07-02 (commit `9f4d889`): la ruta manual (export/scrub) deriva todo de `manualProgress` — helpers puros `easeAppear` (bezier 0.4,0,0.2,1 resuelto por Newton-Raphson, misma curva que live) y `manualAppearStyle` en `appearing.ts` (11 tests); `AppearingCredits` en modo manual renderiza un div plano sin `AnimatePresence` (entradas congeladas por frame; cambios de item instantáneos: las salidas no tienen cuota de timeline y quedan fuera del MP4); `LinesItem` congela cada línea en su punto del escalonado (`manualTimeIntoItem` reemplaza a `manualRevealedLines`; `revealedLinesAt` eliminado, sin consumidores); cursor del typewriter estático en manual. Ruta live intacta. Pendiente verificación interactiva del usuario (scrub + MP4).
- [x] "Cancelar" durante la codificación. Resuelto 2026-07-02 (commit `c1c5002`): `cancelExport` hace `ffmpeg.terminate()` (el `exec` pendiente rechaza) y descarta la instancia (la siguiente exportación recarga ffmpeg); el catch reporta cancelación en vez de error si `cancelRef` está activo.
- [x] `eslint.config.mjs` restaurado a defaults 2026-07-02 (commit `9be48cc`): presets Next planos + `credits-portable/**` en ignores. Aflorado y arreglado: 12 imports/vars sin uso, comillas sin escapar en `CreditEditor`, 5 `any` del test de `getVariants` (tipo estructural `VariantShape`), dep `config` completa en el `useMemo` de `itemDurations`, `MountSignal` actualiza su ref en efecto. 4 setState-in-effect intencionales (resets / derivación de scrub) y la lectura de ETA en render de `ExportDialog` quedan con `eslint-disable` puntual justificado. `pnpm lint` limpio.
- [x] Archivos muertos eliminados 2026-07-02 (commit `1433e7f`): `ui/toast.tsx`, `hooks/use-toast.ts`, `ui/sheet.tsx`, `ui/skeleton.tsx`, `hooks/use-mobile.ts`.
- [x] Config muerta eliminada 2026-07-02 (commit `fd25930`): `showAllAtOnce`, `textShadow` legacy e interface `CreditProject`.
- [x] `SYSTEM_FONTS` completado 2026-07-02 (commit `344717c`): pestaña "Sistema" en `FontManager` (añade la fuente sin inyección de stylesheet); helper puro `buildSystemFontFamily` (comilla nombres con espacios + fallback genérico por fuente, 4 tests); `buildCombinedGoogleFontUrl` eliminado (solo lo usaba su test).
- [x] `importProject` validado 2026-07-02 (commit `dbf0eb6`): rechaza `items` que no sea array de items bien formados (id/text string, type conocido) y `config` que no sea objeto plano; `fonts` corrupto cae a defaults. 4 tests.
- [x] Menores resueltos 2026-07-02 (commit `d38f36f`): el RAF de `ScrollCredits` se detiene al terminar sin loop; callbacks `useCallback` estables en `CreditPreview` (el `ResizeObserver` ya no se recrea por render); fallback de Abril Fatface a `serif`; imports sueltos eliminados.
- [x] Ejecutar INLINE el plan `docs/superpowers/plans/2026-06-30-line-by-line-reveal-and-interval-mount-timing.md`. Hecho 2026-06-30: 6 tareas TDD, commits `8834dae`/`df3684b`/`e8b5d2f`/`9b3cebb`/`2c58685`. La asunción de `mode="wait"` se confirmó por la verificación en navegador del usuario (escalonado y tecleo fiables en todos los items); no hizo falta el plan B.
- [ ] Importar un CSV/TXT y parsearlo a items (entrada masiva de créditos). Pedido 2026-06-25, fuera del spec del inspector. Mitiga la regresión aceptada de tener que escribir el texto item a item en el inspector.

### GitHub Pages / publicación (pedido 2026-06-26)
- [x] Organizar archivos y carpetas del proyecto antes del primer push. Hecho 2026-09-13 (ver History): scaffold z.ai, flujo portable, `tailwind.config.ts` y el JSON de créditos a `.archive/`; `.gitignore` reescrito. Pedido original: decidir qué entra al repo y qué no (scripts `.ps1` de la raíz, `docs/superpowers/` specs+plans, `state.md`, `.archive/`, `credits-portable.zip`/`credits-portable/`, `download/` huérfana). Revisar `.gitignore` y estructura.
- [x] Revisar y actualizar la documentación (`README.md`). Hecho 2026-09-13: reescrito al stack y funciones actuales (sin Prisma ni `.env`, un solo modo `EXPORT`). Pedido 2026-06-26.
- [x] Publicar en GitHub Pages. Hecho 2026-09-13: repo `memoriainfinita/titulus` + Pages con origen GitHub Actions (`pages.yml`: install, tests, `EXPORT=true pnpm build`, `.nojekyll`, deploy). Primer run verde, HTTP 200 en `memoriainfinita.github.io/titulus`. El script `deploy-pages.ps1` previsto no hace falta. Deploy via gh-pages branch (build `EXPORT=true` + `.nojekyll` imprescindible por `_next/`). Script `deploy-pages.ps1` aún sin crear. Caveats: export de vídeo contacta unpkg/Google Fonts (TODO privacidad); `showSaveFilePicker` ok sobre HTTPS de Pages.
- [x] Paneles izquierdo y derecho sin barra de scroll: resuelto con `min-h-0` en las dos `ScrollArea` (`ConfigPanel.tsx`, `CreditEditor.tsx`).
- [x] Quitar el bloque `"pnpm"` inerte de `package.json` (resuelto 2026-06-23): eliminado; la aprobación de builds la cubre `pnpm-workspace.yaml`.

### Tests (2026-06-23)
- [x] Montado Vitest 4 + happy-dom. Scripts `test`/`test:run`, `vitest.config.ts` (alias `@`). Geometría del scroll extraída a `src/lib/credit/scroll.ts` (puro) y usada en `ScrollCredits.tsx`. 34 tests verdes en `scroll.test.ts`, `store.test.ts` (helpers + reducers + roundtrip import/export), `fonts.test.ts`. UI/FFmpeg/drag&drop fuera del alcance unit.

### Investigar / features pendientes (pedidos 2026-06-22)
- [x] Tiempos personalizados por título. Implementado 2026-06-23 para modo appearing (campo `pauseOverride` en `CreditItem`, sobreescribe la pausa por item; duración de animación global). Modo scroll descartado por decisión del usuario 2026-06-23: en scroll el espaciado se logra añadiendo líneas en blanco entre textos, un timing por item sería redundante.
- [x] Velocidad de scroll: slider `scrollSpeed` (10-300 px/s) operativo en `ConfigPanel` y aplicado en `ScrollCredits`. Verificado 2026-06-23.
- [x] Fuentes personalizadas: `FontManager` completo (catálogo Google Fonts, subida ttf/otf/woff con `@font-face`, activar/eliminar). Verificado 2026-06-23.
- [x] Modo oscuro: implementado 2026-06-23. `next-themes` con `ThemeProvider` (`attribute="class"`, `defaultTheme="system"`) en `layout.tsx`, toggle sol/luna (`theme-toggle.tsx`) en el header. Tematiza la UI del editor; el fondo del escenario sigue siendo configurable aparte.
- [x] Modo appearing: círculos indicadores de progreso. Eliminados 2026-06-23 del todo (preview y export), bloque "Progress indicator" retirado de `AppearingCredits.tsx`.
- [x] Presets propios del usuario. Resuelto 2026-06-24: slice `userPresets` en la store (snapshot del `config`, persistido en localStorage, NO en el export de proyecto) + acciones `saveUserPreset`/`deleteUserPreset`/`applyUserPreset` (aplica con backfill `{ ...DEFAULT_CONFIG, ...preset.config }`). UI: dropdown "Mis presets" en `PresetBar` (aplicar al clic, borrar con `X`) + diálogo de guardar (`Dialog` fuera del menú, bloquea nombres duplicados case-insensitive). Spec+plan en `docs/superpowers/specs|plans/2026-06-24-user-presets*`. Verificado en navegador por el usuario.
- [x] Línea de tiempo (timeline) para navegación mientras se editan los créditos. Resuelto 2026-06-24: `TimelineBar.tsx` (barra scrubbeable, reutiliza `manualProgress` 0-1) bajo el preview. La barra corre su propio `requestAnimationFrame` leyendo un `ref` de progreso (los escenarios no se re-renderizan por frame); arrastrar pausa y mueve, soltar congela el frame, Play reanuda desde ahí (`ScrollCredits` siembra `internalProgress` al salir de modo manual). En aparición: botones anterior/siguiente item + indicador "X/Y" + muescas por item sobre la barra (inicios de `itemProgressBounds`). Módulos puros `safeMargins.ts`/`timeline.ts`. Ver TODOs nuevos de mejoras de timeline.
- [x] Control de los tiempos de fade. Resuelto 2026-06-24 reencuadrado por el usuario como "hacer overridable por item lo global". En aparición la duración del fundido ya era global (`animationDuration`); ahora es override por item (`animationDuration` en `CreditItem`) junto con `animationType` y los tunables (`animSlideDistance/animBlurAmount/animZoomFrom/animZoomTo`). Resolvers puros en `appearing.ts` (`resolveAnimationType/Duration/Tunables`, `override ?? global`); `AppearingCredits` calcula `variants` y la duración por item (incl. detección typewriter por item). También `fontWeight` por item (resolver `resolveFontWeight` en store, aplicado en scroll y aparición). UI por item en `CreditEditor` (subcomponente `AnimationOverrides`, en items de texto e imagen, solo en modo aparición). Tests: +10 (appearing.test.ts, store.test.ts). Suite 117. Ver TODO nuevo de auditoría de overrides por item para lo que falta.
- [x] Bug: en modo scroll tarda mucho hasta que aparece el primer texto. Resuelto 2026-06-23: había un offset doble (translateY inicial + padding superior de `containerHeight`); eliminado el padding superior en `ScrollCredits.tsx`. `config.startDelay` sigue sin usarse (código muerto).
- [x] Resolución de exportación elegible. Resuelto 2026-06-24: selector de multiplicador de escala (×1/×1.5/×2/×3) en `ExportDialog` que muestra las dimensiones resultantes (pares) por cada preset; se pasa como `pixelRatio` en `ExportOptions` y se aplica en el `toPng` de `useVideoExport` (antes `pixelRatio: 1` clavado). El layout lógico (width/height) no cambia, solo el ráster. Filtro ffmpeg `scale=trunc(iw/2)*2:trunc(ih/2)*2` para forzar dimensiones pares (yuv420p/H.264). Aviso en la UI de que más resolución = export más lento. Se eligió multiplicador (no 720/1080/4K) por ser agnóstico al ratio del escenario.
- [x] Exportación muy lenta. Resuelto 2026-09-13 con el motor Rápido junto a FFmpeg (ver History): 4:15 frente a 19:25 en el proyecto de ejemplo. El cuello real eran las fuentes incrustadas por frame, no el encoder. Texto de la investigación de 2026-07-03, conservado:
  Pipeline actual (`useVideoExport.ts`): por frame `toPng` (DOM→SVG→canvas→PNG dataURL) con
  `cacheBust: true` (refetch de recursos por frame) → `fetch(dataUrl)` → MEMFS; FFmpeg-wasm re-decodifica
  los PNG y codifica H.264 por software; todos los PNG en memoria a la vez. Hallazgos clave: (1) la
  captura DOM no obliga a pasar por PNG — html-to-image y snapDOM tienen `toCanvas`; (2) un canvas se
  alimenta directo a WebCodecs vía Mediabunny `CanvasSource` (timestamps explícitos, no-realtime, codec
  `avc`, presets `QUALITY_*`, `prefer-hardware`) sin reescribir el render a canvas. Plan recomendado:
  (a) encoder FFmpeg-wasm → WebCodecs+Mediabunny (npm puro, MPL-2.0; elimina unpkg → ayuda al TODO de
  privacidad; `StreamTarget` escribe en streaming al archivo de `showSaveFilePicker` → ayuda al TODO de
  vídeos accesibles); (b) captura `toPng` → `toCanvas` sin `cacheBust`, benchmark A/B html-to-image vs
  snapDOM (MIT, cachea estilos/fuentes entre capturas, `preCache()`) validando fidelidad (sombras, blur,
  fuentes custom, imágenes). Soporte `VideoEncoder`: Chrome/Edge 94+, Firefox 130+ desktop, Safari 16.4+
  (vídeo); fallback: mantener ruta FFmpeg tras check de soporte, o asumir navegadores modernos.
  Descartados: MediaRecorder/captureStream (realtime, no determinista), Element Capture (experimental
  Chrome-only), server-side (app 100% cliente). Cuello restante tras el cambio: rasterización DOM→canvas
  por frame; la reescritura a canvas (sequentia completa) queda aparcada salvo que siga lento.
  Diseñado 2026-07-04: spec `docs/superpowers/specs/2026-07-04-webcodecs-export-engine-design.md` +
  plan `docs/superpowers/plans/2026-07-04-webcodecs-export-engine.md` (5 tareas, suite objetivo 216).
  Aplazado 2026-09-13 por decisión del usuario: la app se publica con el export FFmpeg actual y WebCodecs queda como mejora futura.
- [ ] Probar el motor Rápido en `memoriainfinita.github.io/titulus` tras el push, y en Firefox/Safari (solo verificado en Chrome headless en local).
- [x] Bug en la ruta FFmpeg: con Google Fonts en el documento, `skipFonts: true` dejaba fuera las fuentes subidas y el vídeo salía con la fuente de reserva (Times). Confirmado 2026-09-13 (616 s para 22 s de vídeo, Times). Resuelto 2026-09-13: FFmpeg usa también `createFrameFontEmbedder`; mismo proyecto con la fuente correcta en 178 s.
- [ ] Motor Rápido: tras el arreglo quedan ~220 ms/frame (medido: 1145 frames en 255 s). Captura aislada ~100 ms; el resto sin desglosar (dos RAF, espera de 16 ms, re-render de React, `add()`). Siguiente candidato si hace falta más velocidad: snapDOM o render directo a canvas (ver investigación de 2026-07-03).
- [x] Bug: en el vídeo exportado se ven los botones de navegación. No reproducible 2026-06-23: el export captura `exportStageRef` (escenario oculto sin controles); el usuario confirmó MP4 limpio tras recompilar. La captura previa con controles era de un build anterior.
- [ ] Privacidad / envío de datos a terceros. Revisión inicial 2026-06-22: sin analytics ni telemetría; `z-ai-web-dev-sdk` se eliminó (dependencia muerta, no se importaba). Únicas conexiones externas (GET, no envían contenido del usuario): `unpkg.com` (FFmpeg core, al exportar) y `fonts.googleapis.com`/`fonts.gstatic.com` (Google Fonts). Pendiente: auditoría más a fondo y decidir si self-hostear FFmpeg y fuentes para no contactar terceros.
- [x] Override por item de fuente, tamaño y color. Implementado 2026-06-23 (plan logo/text-overrides, suite 78→90, este TODO quedó sin actualizar). Campos `fontSize`/`color`/`fontFamily` + también `letterSpacing`/`lineHeight` en `CreditItem` (`types.ts`); resolver puro `resolveTextStyle` (`textStyle.ts`, 7 tests) que aplica override válido ?? global; consumido en `ScrollCredits` y `AppearingCredits` (incl. typewriter); UI por item en `CreditEditor` (Tamaño/Color/Fuente/Interletraje/Interlineado con botón de reset a global).
- [x] Modo aparición: navegación fuera del frame. Superseded 2026-06-25 por el `TimelineBar`: navegar/ver progreso fuera del escenario (no exportable) ya resuelto con muescas por item + botones anterior/siguiente + indicador "X/Y". El intent original (que los puntitos salgan del escenario como línea de tiempo para saltar entre items sin aparecer en el vídeo) queda cubierto; los refinamientos residuales viven en "Timeline: mejoras".
- [x] Vignette/desvanecido sin control. Resuelto 2026-06-23: `vignetteEnabled` + `vignetteHeight` (unificado a 20% para ambos modos); toggle + slider en la sección Efectos de `ConfigPanel`; `ScrollCredits` y `AppearingCredits` respetan el control.
- [x] Opacidad de la sombra de texto. Resuelto 2026-06-23: campo `textShadowOpacity` (0-1) + helper puro `text-shadow.ts` (`resolveTextShadow`, hex→rgba, compartido por ambos modos); slider en Efectos. 6 tests.
- [x] Auditoría de valores hardcodeados de animaciones de aparición. Resuelto 2026-06-23: `getVariants` parametrizado con `animSlideDistance`/`animBlurAmount`/`animZoomFrom`/`animZoomTo`; sliders condicionales según tipo de animación. 5 tests (`getVariants.test.ts`).
- [x] Mostrar opcionalmente los márgenes seguros (title-safe / action-safe). Resuelto 2026-06-24: flag `showSafeMargins` en `CreditConfig` (persistido, backfill por `mergePersistedConfig`); overlay `SafeMarginsOverlay` (dos rectángulos punteados al 90%/80%, `z-20`, `pointer-events-none`) solo en el escenario VISIBLE de `CreditPreview` (no en el escenario oculto de exportación → no se captura); botón toggle (renombrado a "Guía" 2026-06-24) en la barra de controles.
- [x] Respetar (forzar) los márgenes seguros: que el usuario decida si el contenido los respeta. Resuelto 2026-06-24: `config.respectSafeMargins` + helper puro `resolveSafeInset` (`safeMargins.ts`, inset 0.1 título-seguro). A diferencia de la guía, constriñe el contenido (inset horizontal 10%, aplicado DENTRO de `ScrollCredits`/`AppearingCredits` → afecta también al escenario oculto de export, es decir al MP4). Solo horizontal (sin recorte vertical en aparición). Botón "Respetar" en `CreditPreview`.
- [x] Viabilidad GitHub Pages (build verificado 2026-09-13: `EXPORT=true pnpm build` genera `out/` con rutas `/titulus/`; el runtime en Pages se confirma al desplegar): factible como export estático (app 100% cliente tras limpieza z.ai; sin server features ni next/image). Requiere `output: "export"` en `next.config.ts` y `basePath`/`assetPrefix` si es project page. Export de vídeo usa FFmpeg-wasm single-thread (`@ffmpeg/core@0.12.6`, no `core-mt`) → no exige SharedArrayBuffer ni cabeceras COOP/COEP (que Pages no permite fijar); debería funcionar, confirmar en deploy real. Carga core de unpkg + Google Fonts en runtime (ver TODO privacidad). Evaluado 2026-06-23.
- [x] Modo typewriter. Resuelto 2026-06-30. La velocidad ya se expuso 2026-06-23 (`typewriterSpeed` ms/carácter, default 50). La causa raíz del efecto roto (el `setInterval` del tecleo arrancaba al cambiar `currentIndex`, antes de que `<AnimatePresence mode="wait">` montara el item, así que el tecleo corría durante el hueco de salida del anterior y el item aparecía completo; el primero iba por no esperar salida) se arregló con el plan mount-timing: el tecleo pasó a `TypewriterItem`, que se monta al hacerse visible el item, y el avance se ancla a `shownTick`/`MountSignal`. Verificado en navegador por el usuario (teclea en todos los items, no solo el primero).
- [x] Mover el control de bucle. Resuelto 2026-06-23: sacado de `ConfigPanel`; botón "Bucle" (icono Repeat) en la barra de reproducción de `CreditPreview`. Además se cableó `config.loop` también en modo scroll (antes el scroll siempre hacía loop e ignoraba el flag) y el botón se muestra en ambos modos.
- [x] Control de blur del texto. Resuelto 2026-06-23: resolver puro `text-blur.ts` (`resolveTextBlur`, override ≥0 ?? global); `filter: blur()` en scroll, aparición y typewriter; slider global en Efectos + input override por item. 4 tests.
- [x] Drag and drop de items. Resuelto 2026-06-23 (HTML5 nativo): grip envuelto en `<span draggable>` con `onDragStart` (id en `dataTransfer`); cada fila es drop target (`onDragOver`/`onDrop` → `reorderItems`) con borde punteado al pasar por encima. `CreditEditor.tsx`.
- [x] Flash de hidratación al recargar (hard reload): aparecían un instante los `DEFAULT_ITEMS` y luego saltaban al estado persistido. Resuelto 2026-06-23: flag `_hasHydrated` (no persistido) + acción `setHasHydrated` + `onRehydrateStorage` en `store.ts` que lo pone true al rehidratar; `page.tsx` solo renderiza los 3 paneles cuando `_hasHydrated`, con placeholder breve (icono Film pulsando) mientras tanto. El flag arranca false en SSR y en el primer render cliente, así que no hay mismatch de hidratación de React. 1 test (`store.test.ts`, `setHasHydrated`). Verificado en navegador por el usuario.
- [x] Revisar los 8 errores `tsc` preexistentes. Resuelto 2026-06-23: en `useVideoExport.ts`, tipo `crf: number`→`string` (era string en valores y uso; arregló líneas 32-34 y 311), `await ffmpeg.listDir("/")` (se iteraba una Promise; la limpieza de frames previos en MEMFS nunca corría) y `Blob([data as BlobPart])` (Uint8Array sobre SharedArrayBuffer). Los 2 de `examples/websocket/*` resueltos borrando `examples/` (plantilla z.ai). `tsc --noEmit` limpio. Commit `1abee6e`.
- [x] `next.config.ts`: quitados los parches z.ai `typescript.ignoreBuildErrors: true` y `output: "standalone"` (este último sobraba sin el deploy con bun). Resuelto 2026-06-23: `next build` ahora ejecuta el type-check (antes lo saltaba) y pasa limpio.
- [x] Control del salto de línea / wrap del texto. Resuelto 2026-06-24: `config.noWrap` (global) + `item.noWrap` (override) → `whiteSpace: "pre"` y sin `wordBreak` (no envuelve automático, respeta saltos manuales); `config.textBoxWidth` + `item.textBoxWidth` (% del escenario) = max-width de la caja de texto, centrada, independiente de `paddingX`. Todo vía `resolveTextStyle` ampliado (`whiteSpace`/`wordBreak`/`maxWidth`), consumido en scroll, aparición y typewriter. UI global en Tipografía (`ConfigPanel`) y override por item en `CreditEditor`.

### UX inspector (pedido 2026-07-04)
- [ ] Ver la posibilidad de reorganizar las opciones del inspector (globales y por item): hay muchos campos y cuesta navegarlos, aunque están bien ordenados. Brainstorming antes de tocar nada.

### UX / atajos de teclado (pedido 2026-06-24)
- [x] Tecla espacio = play/pause de la previsualización. Resuelto 2026-06-24: listener `keydown` global en `CreditPreview` que togglea `setPlaying` (y limpia el seek al reanudar); helper puro `keyboard.ts` (`isInteractiveTarget`, 5 tests) ignora el evento cuando el foco está en input/textarea/select/button/contenteditable/role=button.

### Timeline: mejoras (pedidas 2026-06-24)
- [ ] Tooltip al pasar el ratón por la barra de tiempo (modo aparición) mostrando el contenido del item bajo el cursor. Mapear la posición X del ratón → fracción → item vía `itemProgressBounds`/`getVisibleItems`; render del texto del item en un tooltip.
- [x] Remarcar el item que se está visualizando y que el resalte vaya cambiando según avanza el pase. Resuelto 2026-06-25 (refactor inspector, Task 8): resalte ámbar de la fila activa en `CreditEditor`, derivado en aparición de `currentItemIndex` y en scroll de la geometría real (`activeItemIndexAtProgress` sobre los offsets medidos), sincronizado con reproducción y con el desplazador.
- [x] Doble clic en un item de la lista del editor lleva la barra de tiempo al momento de ese item. Resuelto 2026-06-25 (refactor inspector, Task 7): `onDoubleClick` en la fila emite `requestSeek(id)`; `CreditPreview` observa `seekTarget` y hace seek (aparición vía `seekToItem`, scroll vía `scrollProgressForItem` sobre la geometría medida).

### Overrides por item (pedidos 2026-06-24)
- [x] Auditoría: hacer overridable por item todo lo global razonable. Completado 2026-06-25 con la última tanda (sombra + typewriterSpeed por item). Hechos 2026-06-24: animación (type/duration/tunables) y peso de fuente. YA estaban: alineación, negrita/cursiva/mayúsculas, tamaño, color, fuente, interletraje, interlineado, blur, pausa (aparición), spacer, divider, ancho de imagen. Hechos 2026-06-25: velocidad de tecleo (`typewriterSpeed`) por item; sombra de texto por item (grupo completo on/color/blur/x/y/opacidad). NO aplica por item (global por naturaleza): modo, fondo/gradiente, vignette, scroll (velocidad/dirección/pausa), paddingX, itemSpacing, escenario (tamaño/ratio), bucle, márgenes seguros.

### Exportación: gestión de archivos y feedback (pedidos 2026-06-24)
- [ ] Videos generados siempre accesibles. Pedido 2026-06-24. Hoy el MP4 solo existe como `Blob` en memoria: tras exportar se dispara una descarga automática (`<a download>`, `ExportDialog.handleExport`) a la carpeta Descargas del SO y se pierde la referencia al cerrar el diálogo. Diseñar una galería/historial in-app de vídeos generados (p. ej. persistir blobs en IndexedDB como hace sequentia con sus proyectos) para re-descargar/previsualizar sin re-exportar. Relacionado con el TODO de elegir destino.
- [x] Mostrar el tiempo real de exportación transcurrido. Resuelto 2026-06-24. Módulo puro `src/lib/credit/exportTiming.ts` (`formatElapsed` m:ss, `estimateRemainingSeconds` por extrapolación lineal; 11 tests). `ExportDialog` arranca `startTimeRef` al iniciar, un `setInterval(250ms)` actualiza `elapsedMs` mientras `isExporting` (se congela en done/error vía `finally`). La fila bajo la barra muestra `transcurrido · restante ~ETA` (ETA solo en fases capturing/encoding, oculto durante la descarga de ffmpeg); el cuadro verde final dice "Video exportado en m:ss".
- [x] Auditar el export de PROYECTO (JSON): ¿se exporta toda la configuración? Auditado 2026-06-24: `exportProject` (`store.ts`) hace `JSON.stringify({ version, projectName, items, config, fonts })` con el objeto `config` y el array `items` COMPLETOS — no hay lista blanca de campos, así que toda clave nueva de `CreditConfig`/`CreditItem` entra automáticamente; `importProject` rehidrata con `{ ...DEFAULT_CONFIG, ...parsed.config }` e `items` tal cual. Verificado con test de roundtrip de las claves nuevas (`store.test.ts`). Sin huecos.
- [x] Bug: el tamaño aproximado calculado está mal (decía ~135 MB, real 7,6 MB). Resuelto 2026-06-24 por decisión del usuario: como el encode es CRF (calidad constante) y los créditos son muy comprimibles, cualquier estimación previa engaña. Se quitó la tarjeta "Tamaño aprox." del resumen (grid 2×2 → 3 columnas: Duración/Resolución/Frames) y se eliminó la fórmula `estimatedSizeMB`. El tamaño REAL se sigue mostrando al terminar (badge + toast).
- [x] Elegir dónde se guardan los vídeos (no descargar a Descargas automáticamente). Resuelto 2026-06-24: `handleExport` abre `showSaveFilePicker` (File System Access API) **al pulsar Iniciar** (requisito de gesto de usuario, antes del encode largo), guarda el `FileSystemFileHandle` y al terminar escribe el blob con `createWritable()`. Si el usuario cancela el selector se aborta la exportación en silencio (`AbortError`); en navegadores sin soporte (Firefox/Safari) o si la escritura falla, fallback a descarga automática (`triggerDownload`). El cuadro final distingue "Guardado en la ubicación que elegiste" vs "Se ha descargado automáticamente".
- [x] Aclarar la carpeta `download/` del proyecto. Investigado 2026-06-24: es un resto del scaffold z.ai (su `README.md` dice "Here are all the generated files."); contiene MP4s de ejemplo viejos del 2026-06-22. **No la usa nadie**: cero referencias en `src/`, y la app descarga los vídeos vía blob a la carpeta Descargas del SO, no aquí. Está en `.gitignore` (`/download/`). Es huérfana; se puede borrar sin impacto (no la borro aún por si el usuario quiere conservar esos ejemplos).
